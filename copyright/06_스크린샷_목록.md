# 스크린샷 목록 — Insight Bank Hub

> 한국저작권위원회 제출 시 첨부하는 실행 화면 스크린샷 가이드.
> 14개 모듈 + 헤더/푸터/언어 전환 화면 = **약 20장 권장**.

---

## 1. 권장 환경
- **브라우저**: Microsoft Edge 또는 Chrome (최신)
- **해상도**: 1440 × 900 (Desktop) — 권장
- **추가**: 375 × 812 (Mobile / iPhone 12) — 모바일 우선 설계 입증
- **포맷**: PNG (무손실, 한국저작권위원회 권장)

---

## 2. 촬영 대상 (20장 표준)

### 2.1 메인 진입 (1장)
| # | 페이지 | URL | 핵심 영역 |
|---|--------|-----|----------|
| 01 | 메인 | `index.html` | Hero + KPI + 외부 매체 협업 안내 |

### 2.2 헤더·푸터 일관성 (2장)
| # | 항목 | 내용 |
|---|------|------|
| 02 | 헤더 메뉴 펼침 | "제안" 드롭다운 5개 링크 펼친 상태 |
| 03 | 푸터 5 그룹 | 푸터의 5 컬럼 사이트맵 (브랜드 + 제안 + 기술 + 데모 + 안내) |

### 2.3 제안·영업 (5장)
| # | 페이지 | URL | 핵심 영역 |
|---|--------|-----|----------|
| 04 | 계약 제안서 | `proposal.html` | 12개월 도입 로드맵 |
| 05 | 6개 채널 | `channels.html` | 6 매체 카드 + 확장 협업 안내 |
| 06 | 상품·서비스 | `products.html` | 36종 카탈로그 |
| 07 | 수익 모델 | `monetization.html` | 9 Stream + **확장 BM 4-Tier** |
| 08 | 경쟁 환경 | `competitive-landscape.html` | 40+ 경쟁사 비교 |

### 2.4 기술·차별화 (3장)
| # | 페이지 | URL | 핵심 영역 |
|---|--------|-----|----------|
| 09 | 시스템 구조도 | `structure.html` | 4-Layer SVG |
| 10 | 온톨로지 | `ontology.html` | FIBO/XBRL · 도메인 그래프 |
| 11 | 그래프 라이선스 | `graph-license.html` | Stream 09 4티어 가격 |

### 2.5 플랫폼·데모 (4장)
| # | 페이지 | URL | 핵심 영역 |
|---|--------|-----|----------|
| 12 | 통합 대시보드 | `dashboard.html` | 6채널 KPI + AI 인용 추이 |
| 13 | 콘텐츠 관리 | `content.html` | 발행 큐 + GEO 점수 |
| 14 | RAG 지식베이스 | `knowledge.html` | 벡터 인덱스 + 토픽 |
| 15 | RAG 챗봇 | `chatbot.html` | 응답 화면 + 출처 인용 |

### 2.6 매뉴얼·다국어 (3장)
| # | 항목 | 내용 |
|---|------|------|
| 16 | 운영 매뉴얼 | `manual.html` 시작하기 + TOC |
| 17 | 언어 전환 | 우측 상단 🌐 언어 선택 메뉴 펼침 |
| 18 | 영문 표시 | 언어를 EN으로 전환한 index 화면 |

### 2.7 모바일 (2장)
| # | 항목 | 해상도 |
|---|------|--------|
| 19 | 모바일 메인 | 375×812 — index.html |
| 20 | 모바일 메뉴 | 375×812 — 햄버거 메뉴 펼친 상태 |

---

## 3. 자동 촬영 명령

### 3.1 PowerShell — 데스크톱 전체 화면
```powershell
Add-Type -AssemblyName System.Windows.Forms, System.Drawing
$bounds = [System.Windows.Forms.Screen]::PrimaryScreen.Bounds
$bitmap = New-Object System.Drawing.Bitmap($bounds.Width, $bounds.Height)
$graphics = [System.Drawing.Graphics]::FromImage($bitmap)
$graphics.CopyFromScreen($bounds.Location, [System.Drawing.Point]::Empty, $bounds.Size)
$bitmap.Save('C:\01 Claude-Code\10-19 Insight Bank Hub (인사이트 뱅크 허브)\copyright\screenshots\01_index.png',
             [System.Drawing.Imaging.ImageFormat]::Png)
```

### 3.2 puppeteer-core — 14개 페이지 자동 촬영
```javascript
// node copyright/auto-screenshot.js
const puppeteer = require('puppeteer-core');
const path = require('path');

(async () => {
  const browser = await puppeteer.launch({
    executablePath: 'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe',
    defaultViewport: { width: 1440, height: 900 }
  });

  const pages = [
    'index', 'proposal', 'channels', 'products', 'monetization',
    'competitive-landscape', 'structure', 'ontology', 'graph-license',
    'dashboard', 'content', 'knowledge', 'chatbot', 'manual'
  ];
  const baseDir = 'C:\\01 Claude-Code\\10-19 Insight Bank Hub (인사이트 뱅크 허브)';
  const outDir = path.join(baseDir, 'copyright', 'screenshots');

  for (const name of pages) {
    const page = await browser.newPage();
    await page.goto('file:///' + path.join(baseDir, name + '.html').replace(/\\/g, '/'));
    await page.screenshot({ path: path.join(outDir, name + '.png'), fullPage: true });
    await page.close();
  }
  await browser.close();
})();
```

### 3.3 Edge Headless — 풀페이지 PDF + 스크린샷
```powershell
$pages = @('index','proposal','channels','products','monetization',
           'competitive-landscape','structure','ontology','graph-license',
           'dashboard','content','knowledge','chatbot','manual')
$base = "C:\01 Claude-Code\10-19 Insight Bank Hub (인사이트 뱅크 허브)"
$out = Join-Path $base "copyright\screenshots"
New-Item -Path $out -ItemType Directory -Force | Out-Null
foreach ($p in $pages) {
  $url = "file:///" + (Join-Path $base "$p.html").Replace('\','/')
  & "msedge.exe" --headless --disable-gpu --window-size=1440,900 --screenshot="$out\$p.png" $url
}
```

---

## 4. 제출 가이드

### 4.1 파일명 규칙
- `01_index.png`, `02_header_menu.png`, …, `20_mobile_menu.png` (번호 + 영문 식별자)
- 한국어 식별자가 필요하면 `01_메인.png` 등 — 단, 압축 시 인코딩 주의

### 4.2 압축 및 첨부
- 20장 PNG → `screenshots.zip` 으로 압축
- 한국저작권위원회 온라인 등록 시 "기타 첨부 자료"로 업로드

### 4.3 캡션 작성 (각 스크린샷 하단)
> 권장 형식: `[페이지명] — [핵심 영역 한 줄 설명]`
> 예: `메인 (index.html) — Hero + KPI 4종 + 외부 매체 협업 안내`

---

## 5. 권장 추가 촬영 (편집저작물 입증 강화용)

| 추가 항목 | 의의 |
|----------|------|
| 헤더 5 그룹 모두 펼친 비교샷 | 5 그룹 메뉴 위계의 일관성 |
| 푸터 5 그룹 사이트맵 클로즈업 | 헤더-푸터 일관 적용 입증 |
| 8개 언어 모두 전환한 헤더 | 다국어 편집 입증 |
| RTL(아랍어) 적용 화면 | RTL 부분 적용 규칙 입증 |
| products.html 인쇄 미리보기 | @media print 편집 입증 |
| graph-license.html 4티어 가격 | Stream 09 가격 배열 입증 |
| monetization.html 확장 BM 4-Tier | 외부 매체 협업 BM 입증 |
